---
name: Compile PureData Object
on:
    workflow_dispatch:
        inputs:
            publish_release:
                description: Publish a GitHub Release using LIBVERSION after build
                required: false
                type: boolean
                default: false
env:
    LIBNAME: p2p~
    LIBVERSION: "0.1.0"
permissions:
    contents: write
jobs:
    macos:
        runs-on: ${{ matrix.machines }}
        strategy:
            matrix:
                precision: [32, 64]
                machines: [macos-latest]
        steps:
            - uses: actions/checkout@v4
              with:
                  submodules: recursive
                  fetch-depth: 0
            - name: Install PureData and Deps
              run: |
                  brew install pd
                  brew install boost
                  ls -la $(brew --prefix boost)/include/boost/lockfree
                  find $(brew --prefix boost)/include -name spsc_queue.hpp
            - name: Build
              run: |
                  export CXXFLAGS="-I$(brew --prefix boost)/include"
                  cmake . -B build -DPD_FLOATSIZE=${{ matrix.precision }} -DPD_LIB_DIR=./ -DCMAKE_BUILD_TYPE=Release -DCMAKE_OSX_ARCHITECTURES="arm64"       -DBOOST_ROOT=$(brew --prefix boost) -DBoost_INCLUDE_DIR=$(brew --prefix boost)/include
                  cmake --build build --parallel
            - name: Upload ONNX Folder
              uses: actions/upload-artifact@v4
              with:
                  name: ${{ env.LIBNAME }}-${{ env.LIBVERSION }}-pd${{ matrix.precision }}-${{ matrix.machines }}
                  path: build/${{env.LIBNAME}}
    linux:
        runs-on: ubuntu-latest
        strategy:
            matrix:
                arch: [amd64]
                precision: [32, 64]
        steps:
            - uses: actions/checkout@v4
              with:
                  submodules: recursive
                  fetch-depth: 1
            - name: PureData Sources
              run: |
                  sudo apt update
                  sudo add-apt-repository ppa:pure-data/pure-data -y
                  sudo apt install libboost-all-dev puredata -y
            - name: Install aarch64 gcc
              if: matrix.arch == 'aarch64'
              run: |
                  sudo apt install gcc-aarch64-linux-gnu -y
                  sudo apt install g++-aarch64-linux-gnu -y
                  cmake . -B build -DPD_FLOATSIZE=${{ matrix.precision }} -DCMAKE_SYSTEM_PROCESSOR=${{ matrix.arch }} -DPD_LIB_DIR=./ -DCMAKE_BUILD_TYPE=Release -DCMAKE_C_COMPILER=aarch64-linux-gnu-gcc -DCMAKE_CXX_COMPILER=aarch64-linux-gnu-g++
            - name: Install arm gcc
              if: matrix.arch == 'arm'
              run: |
                  sudo apt install gcc-arm-linux-gnueabihf -y
                  sudo apt install g++-arm-linux-gnueabihf -y
                  cmake . -B build -DPD_FLOATSIZE=${{ matrix.precision }} -DCMAKE_SYSTEM_PROCESSOR=${{ matrix.arch }} -DPD_LIB_DIR=./ -DCMAKE_BUILD_TYPE=Release -DCMAKE_C_COMPILER=arm-linux-gnueabihf-gcc -DCMAKE_CXX_COMPILER=arm-linux-gnueabihf-g++
            - name: Configure
              if: matrix.arch == 'amd64'
              run: |
                  cmake . -B build -DPD_FLOATSIZE=${{ matrix.precision }} -DCMAKE_SYSTEM_PROCESSOR=${{ matrix.arch }} -DPD_LIB_DIR=./ -DCMAKE_BUILD_TYPE=Release
            - name: Build Object
              run: |
                  cmake --build build -- -j$(nproc)
            - name: Upload Object
              uses: actions/upload-artifact@v4
              with:
                  name: ${{ env.LIBNAME }}-${{ env.LIBVERSION }}-pd${{ matrix.precision }}-linux-${{ matrix.arch }}
                  path: build/${{env.LIBNAME}}
    windows:
        runs-on: windows-latest
        strategy:
            matrix:
                compiler: [mingw]
                arch: [amd64]
                precision: [32, 64]
        steps:
            - name: Checkout repository
              uses: actions/checkout@v4
              with:
                  submodules: recursive
                  fetch-depth: 0
            - if: matrix.compiler == 'mingw'
              name: Set up Msys2
              uses: msys2/setup-msys2@v2
              with:
                  msystem: mingw64
                  install: make mingw-w64-x86_64-gcc mingw64/mingw-w64-x86_64-cmake mingw-w64-x86_64-boost mingw-w64-x86_64-mbedtls
                  update: false
            - name: Install PureData Float 32
              if: matrix.precision == '32'
              run: |
                  winget install -e --id MillerPuckette.PureData --accept-source-agreements
            - name: Install PureData Float 64
              if: matrix.precision == '64'
              run: |
                  winget install -e --id MillerPuckette.Pd64 --accept-source-agreements
            - name: Configure and build Mingw
              shell: msys2 {0}
              if: matrix.compiler == 'mingw'
              run: |
                  cmake . -B build -DPD_FLOATSIZE=${{ matrix.precision }} -DCMAKE_SYSTEM_PROCESSOR=${{ matrix.arch }} -DPD_LIB_DIR=./ -DCMAKE_BUILD_TYPE=Release 
                  cmake --build build --parallel
            - name: Upload
              uses: actions/upload-artifact@v4
              with:
                  name: ${{ env.LIBNAME }}-${{ env.LIBVERSION }}-pd${{ matrix.precision }}-windows-${{ matrix.arch }}
                  path: build/${{env.LIBNAME}}

    publish-release:
        if: ${{ github.event.inputs.publish_release }}
        permissions:
            contents: write
        needs: [macos, linux, windows]
        runs-on: ubuntu-latest
        steps:
            - name: Download puredata
              uses: actions/download-artifact@v4
              with:
                  pattern: p2p~-*
                  merge-multiple: true
                  path: ${{ env.LIBNAME }}

            - name: Upload using docker deken
              env:
                  DEKEN_USERNAME: charlesneimog
                  DEKEN_PASSWORD: ${{ secrets.DEKEN_PASSWORD }}
              run: |
                  docker run --rm \
                    --user $(id -u) \
                    --volume $(pwd):/deken \
                    registry.git.iem.at/pd/deken \
                    deken package --version ${{ env.LIBVERSION }} ${{ env.LIBNAME }}

                  docker run --rm \
                    --user $(id -u) \
                    --volume $(pwd):/deken \
                    -e DEKEN_USERNAME -e DEKEN_PASSWORD \
                    registry.git.iem.at/pd/deken \
                    deken upload -v ${{ env.LIBVERSION }} *.dek

            - name: Create zip
              run: |
                  zip -r p2p~-${{ env.LIBVERSION }}.zip ${{ env.LIBNAME }}

            - name: Create / Update GitHub Release
              uses: softprops/action-gh-release@v2
              with:
                  tag_name: v${{ env.LIBVERSION }}
                  name: v${{ env.LIBVERSION }}
                  target_commitish: ${{ github.sha }}
                  draft: true
                  prerelease: false
                  files: |
                      p2p~-${{ env.LIBVERSION }}.zip
